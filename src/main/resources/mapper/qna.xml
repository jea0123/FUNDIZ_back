<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.funding.mapper.UserMapper">

    <select id="getQnAList" parameterType="long" resultType="com.example.funding.dto.response.user.MyPageQnADto">
        SELECT
        <!-- QnA 테이블-->
        q.qnaId,
        q.projectId,
        q.userId,
        q.creatorId,
        q.projectId,
        q.createdAt,
        <!-- 창작자 테이블 -->
        c.creatorName

        FROM qna q
        <!-- 테이블 조인 -->
        JOIN creator c ON q.creatorId = c.creatorId
        JOIN users u ON q.userId = q.userId
        JOIN project p ON p.projectId = q.projectId
        WHERE u.userId=#{userId}
        ORDER BY createdAt

    </select>


</mapper>

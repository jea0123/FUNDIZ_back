<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.funding.mapper.QnADetailMapper">

    <select id="getQnAById" parameterType="map" resultType="com.example.funding.model.Qna">
        SELECT
        <!-- QnA 테이블-->
        q.qnaId,
        q.projectId,
        q.userId,
        q.creatorId,
        q.title,
        q.content,
        q.createdAt


        <!--창작자 테이블-->
        <!--c.creatorName,-->

        <!-- 프로젝트 테이블-->
        <!--p.title AS projectTitle,-->
        <!--유저 테이블-->
        <!--u.nickName-->

        FROM qna q
        <!-- 테이블 조인 -->
        <!--JOIN creator c ON q.creatorId = c.creatorId
        JOIN users u ON q.userId = u.userId
        JOIN project p ON p.projectId = q.projectId-->
        WHERE userId=#{userId}
        AND projectId =#{projectId}
        ORDER BY createdAt

    </select>
</mapper>
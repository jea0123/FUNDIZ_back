<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.funding.mapper.InquiryMapper">

    <select id="inquiryList" resultType="inquiry">
        SELECT * FROM
        (SELECT sub.*, ROWNUM rnum FROM (
        SELECT * FROM inquiry order by INQID desc
        ) sub)
    </select>

    <select id="inquiryDetail" resultType="inquiry">
        SELECT * FROM inquiry
        WHERE INQID = #{inqId}
    </select>

    <select id="item" resultType="inquiry">
        SELECT * FROM inquiry
        WHERE INQID=#{inqId}
    </select>

    <insert id="addInquiry" parameterType="com.example.funding.dto.request.cs.IqrAddRequestDto">
        <selectKey keyProperty="inqId" resultType="Long" order="BEFORE">
            SELECT INQUIRY_SEQ.NEXTVAL FROM DUAL
        </selectKey>

        INSERT INTO inquiry (INQID, USERID, ADID, TITLE, CONTENT, CREATEDAT, ISCANCELED, CTGR, ISANSWER)
        VALUES (inquiry_seq.NEXTVAL, 500, 10, #{title}, #{content}, SYSDATE, 'N', #{ctgr}, 'N')
    </insert>

</mapper>
